# 地勤登机口工作流程提醒小程序设计指南

## 品牌定位

- **应用名称**：登机口工作流程助手
- **目标用户**：航空公司地勤登机口工作人员
- **设计理念**：专业、高效、清晰——在高压工作环境下确保每个步骤不遗漏

## 视觉风格

- **整体风格**：航空专业感 + 工业极简
- **意象锚点**：机场航站楼的清晰标识系统——蓝底白字、简洁线条、高对比度

## 配色方案

- **主色（航空蓝）**：`bg-blue-600`（#2563eb）- 信任、专业
- **成功色**：`bg-green-500`（#22c55e）- 已完成状态
- **警告色**：`bg-amber-500`（#f59e0b）- 待处理/注意
- **背景色**：`bg-gray-50` - 浅灰底
- **文字色**：主文字 `text-gray-900`，次要 `text-gray-600`
- **卡片色**：`bg-white` - 纯白卡片

## 字体规范

- 标题：`text-xl font-bold`
- 正文：`text-base`
- 辅助：`text-sm text-gray-500`

## 间距系统

- 页面边距：`px-4`
- 卡片内边距：`p-4` 或 `p-5`
- 组件间距：`gap-3` 或 `gap-4`

## 组件使用原则

- 通用按钮使用 `@/components/ui/button`
- 输入框使用 `@/components/ui/input`
- 卡片使用 `@/components/ui/card`
- 进度展示使用 `@/components/ui/progress`
- 弹窗使用 `@/components/ui/dialog`
- 表单使用 `@/components/ui/field`
- Toast 使用 `@/components/ui/sonner`

## 页面结构

### 首页（航班号入口）
- 大标题显示应用名称
- 航班号输入框（支持大写字母+数字）
- "开始流程"按钮
- 底部"内容管理"入口

### 工作流程页面
- 顶部显示当前航班号
- 进度条显示完成进度
- 当前提醒内容卡片（突出显示）
- "确认"按钮
- 已完成项目列表（折叠）

### 内容管理页面
- 提醒项目列表
- 每个项目可编辑
- 支持新增/删除/排序
- 保存按钮

## 小程序约束

- 包体积限制：资源走 TOS 存储
- 保持 UI 简洁，减少动画
- 操作反馈明确（Toast 提示）
